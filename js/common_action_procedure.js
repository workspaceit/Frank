function add_to_favourite(element,email){    load_notification_bar("Processing..");    var url=$('#site_url').val()+"user_favorite/submit_insert_data";     $.ajax({            url:url,            type:"post",            data:{                 email:email            },            success:function(data){               var resp=JSON.parse(data);               console.log(resp);               chage_notification_content(resp.msg);               show_success_then_hide(2000,"");               //$(element).parents('.dummy_add_f_btn').first().fadeOut(500);            },            error:function(){               chage_notification_content("Internal Server Error");             //  show_error_then_hide(2000,"my_profile");            }        });}function resizeBackground(subHeight){    var body = document.body,        html = document.documentElement;    var height = Math.max( body.scrollHeight, body.offsetHeight,            html.clientHeight, html.scrollHeight, html.offsetHeight )-subHeight;    $("#content").height(height+"px");}function redirectForNewGossip($uid){    window.location = $('#site_url').val()+"profile/view/"+$uid+"?action=new_gossip";}function removeFromGroup(group_id,member_id){    var reply = confirm('Are you sure to delete member?');    if(reply){        $.ajax({            url : BASE+'group_members/remove_group_member',            type: 'POST',            data : { group_id : group_id, member_id : member_id },            success : function(r) {                if(r) {                    location.reload();                }            }        });    }}function removeFromFev(member_id){    reply = confirm('Are you sure to delete member?');    if(reply){        $.ajax({            url : BASE+'group/remove_favorite_member',            type: 'POST',            data : { member_id : member_id },            success : function(r) {                if(r) {                    location.reload();                }            }        });    }}